server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: git@github.com:diegosg05/store-microservices.git
          default-label: main
          search-paths: config-repo


# Configuración del Cliente Eureka para que el Config Server se registre
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    instance-id: ${spring.application.name}:${random.value}
